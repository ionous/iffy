package chart

import (
	// "github.com/ionous/errutil"
	// testify "github.com/stretchr/testify/assert"
	"testing"
)

// FIX: add tests for implicit directives ( which dont allow trailing trim )
func TestDir(t *testing.T) {

	// "{}"

	// test := func(str string) (err error, ret interface{}) {
	// 	var p quoteParser
	// 	parse(&p, str)
	// 	if n, e := p.GetString(); e != nil {
	// 		err = e
	// 	} else if n != str {
	// 		err = errutil.New("mismatched strings", str, n)
	// 	}
	// 	return err, str
	// }
	// assert := testify.New(t)
	// _ = assert.NoError(test("'singles'")) &&
	// 	assert.NoError(test(`"doubles"`)) &&
	// 	assert.NoError(test("'escape\"'")) &&
	// 	assert.NoError(test(`"\\"`)) &&
	// 	assert.NoError(test(string([]rune{'"', '\\', 'a', '"'}))) &&
	// 	assert.Error(test(string([]rune{'"', '\\', 'g', '"'})))

}

//epilogue
