// Package rt defines the runtime interfaces used to interact with the world.
// Package ref manipulates objects, package rt manipulates ref.
// We use interfaces rather than concrete implementations to decouple package dependencies.
package rt
